runtime of my wiggleSort function analysis:

runtime of sort function is O(n^2)
each of the two loops in the function have a runtime of O(n) for a total of O(2n)
this calculates to O(n^2) + O(2n) + constants, which i simplify to O(n^2) as my runtime.